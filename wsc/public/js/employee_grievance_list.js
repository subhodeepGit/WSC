// frappe.listview_settings['Employee Grievance'] = {
//     onload: function(listview) {
//         var user = frappe.session.user;
//         alert("hello")
//         // Check if the user has the role "HR Admin" or "Grievance Cell Member".
//         if (frappe.user.has_role(user, "HR Admin") || frappe.user.has_role(user, "Grievance Cell Member")) {
            
//             // Ignore user permission for the whole doctype.
//             listview.ignore_permissions = true;
//         }
//     }
// };
