frappe.listview_settings['Entrance Exam Admit Card'] = {
    refresh: function(listview) {
        $(".btn-primary").hide()
    },
    onload: function(listview) {
        $(".btn-primary").hide()
    }
}